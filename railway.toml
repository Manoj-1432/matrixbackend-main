[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "php artisan config:cache && php artisan route:cache && php artisan migrate --force && php -S 0.0.0.0:$PORT -t public public/server.php"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
